# LTPP owner-corner carrier MVP v3 candidate correction — 2026-08-10

Before any owner point was saved or locked in the v2 packet, an eight-date
candidate contact sheet exposed two topology errors. The v1 and v2 packets
remain retained with zero points.

The candidate-only corrections are:

- `19910610 TL/TR`: `(218, 242)` and `(2582, 176)` followed the first interior
  dashed row; replace them with `(218, 150)` and `(2582, 83)` on the slanted
  outer top solid line.
- `19970228 BL`: `(519, 897)` created a false diagonal left edge; replace it
  with `(592, 897)` at the outer left/bottom solid-line intersection.

All other suggested points and every claim boundary in the original
preregistration and v2 amendment remain unchanged. These corrected suggestions
still require explicit owner adoption or replacement and locking. No transform
or final audit is run.
