# D1 remaining-matrix launch authorization

**Date:** 2026-08-26  
**Owner authorization:** explicit annotation `授权` on the statement that the
remaining 11 jobs had not yet been authorized  
**Status:** `AUTHORIZED_REMAINING_11_UNCHANGED_JOBS`

## Scope

Authorize the 11 jobs remaining after the completed hard-5/seed-1 producer
smoke. This authorization changes only the execution state. It does not change
the frozen dataset, folds, seeds, model, optimization, warning threshold,
controls, evaluation origins, aggregation rule, success gate or claim class.

The machine-readable matrix lock remains byte-for-byte unchanged so all 12
jobs retain the same scientific lock hash. Its historical
`launch_authorized=one_full_job_smoke_only_after_revision` field is superseded
operationally by this dated owner authorization; it is not rewritten post hoc.

## Five-question experiment gate

1. **Mechanism question:** across four whole-trajectory holdouts and three
   seeds, does transition-balanced GNO-data outperform the locked data-only
   temporal controls on both warning and predicted fields?
2. **Claim change:** a pass supports only a fast surrogate for processed
   archived GRIPHFiTH outputs; a fail says these four trajectories and this
   frozen supervised operator are insufficient. Neither outcome qualifies the
   FEM teacher or physical truth.
3. **Cheaper diagnostic:** the independent code review, 17 focused tests and
   one exact 3,000-step Taobo producer/asset smoke have completed.
4. **Minimal asset:** 12-job completeness table, seed-median fold table,
   paired-control table and a bounded decision note, in addition to each
   job's locked assets.
5. **Registry endpoint:** update the D1 preregistration/amendment and canonical
   track only after the aggregate is sealed; retain bulky outputs in the local
   archive and Taobo archive root.

## Non-authorized work

- Request 30 or any new FEM export;
- physics, equilibrium, AT1, KKT or history-update loss;
- threshold, mask, origin, control or aggregation changes;
- claims of calibrated probability, qualified teacher, physical validation,
  prospective warning or real-road posterior.
