# Aligned FEM/PIDL Reference Tiers

Date: 2026-05-30

## Question

After aligning the clean soft-hist0 FEM setting, does the remaining FEM/PIDL gap
depend on whether the FEM reference is the standard cadence or the Request 21
`n_step=10` cadence?

## Source

Generated by:

```text
SENS_tensile/analyze_aligned_fem_pidl_reference_tiers.py
```

Inputs:

```text
_analysis_fem_mechanism_20260528/femmesh_pidl_vs_soft_hist0_fem_probe_j0map_c1_c20_c40_c69.csv
_analysis_fem_mechanism_20260528/femmesh_pidl_vs_soft_hist0_incremental_energy_c1_ref.csv
_analysis_fem_mechanism_20260528/request21_fem_nstep/source_handoff/_pidl_handoff_reverseBC_u12_soft_hist0_nstep10_2026-05-29/
SENS_tensile/meshed_geom_fem_soft_hist0.msh
```

Outputs:

```text
_analysis_fem_mechanism_20260528/aligned_fem_pidl_reference_tiers.csv
_analysis_fem_mechanism_20260528/aligned_fem_pidl_energy_tiers.csv
_analysis_fem_mechanism_20260528/aligned_fem_pidl_reference_tier_summary.csv
_analysis_fem_mechanism_20260528/figures/aligned_fem_pidl_reference_tiers_20260530.png
_analysis_fem_mechanism_20260528/femmesh_pidl_fixed_vs_event_alignment_summary.csv
_analysis_fem_mechanism_20260528/figures/femmesh_pidl_fixed_vs_event_alignment_20260530.png
```

## Protocol

This is a fixed-cycle comparison:

```text
FEM c -> PIDL saved j = c - 1
cycles: c1, c20, c40, c69
references: soft-hist0 standard FEM, soft-hist0 n_step=10 FEM
PIDL: FEM-mesh soft-hist0 archive
fields: d/alpha, alpha_bar, raw psi+, active psi+, f
metrics: max, p99, p999, tip_2ell mean/integral, right-band mean
energy: absolute E_d and Delta E_d from c1 kept separate
```

The n_step10 FEM fields were projected to the same PIDL triangular probes using
the same element-centroid assignment logic as the earlier standard-FEM
common-probe comparison.  Assignment fallback was `0/45000`.

## Result

Changing the aligned FEM reference from standard cadence to `n_step=10` does
not remove the PIDL mechanism gap.

At c69:

| Reference | damage tip2 | alpha_bar tip2 | alpha_bar p99 | raw psi tip2 | active psi tip2 | Delta E_d |
|---|---:|---:|---:|---:|---:|---:|
| standard FEM | 1.012 | 0.466 | 0.191 | 1.330 | 0.0287 | 0.2505 |
| n_step10 FEM | 1.008 | 0.373 | 0.202 | 1.583 | 0.0233 | 0.2500 |

The numbers are PIDL/FEM ratios on common probes.  Damage location/amount near
the tip is essentially aligned, but history and active/degraded driver are not.
The raw tensile driver can be comparable or even larger in PIDL, while the
active driver collapses after degradation.  This is the same mechanism
interpretation as before:

```text
PIDL forms damage in roughly the right place,
but the degraded active work feeding fatigue history is too small near the tip.
Then alpha_bar and incremental E_d fall behind FEM as cycles accumulate.
```

## Caveats

The `n_step=10` scalar CSV has near-zero `E_el` because of the retained/exported
energy-row convention in that handoff.  This note uses `E_d` and field
reductions from the MAT element fields; do not use the n_step10 `E_el` scalar
for elastic-energy conclusions without a refreshed FEM energy export.

The strict matched-event comparison for FEM cadence variants is already in:

```text
docs/fem_request21_cadence_interpretation_2026-05-30.md
```

For PIDL matched-event fields, Mac pulled the Taobo FEM-mesh checkpoint files
for saved states j84 and j85 from:

```text
/mnt/data2/drtao/projects/pidl-align-soft-hist0-20260529/SENS_tensile/..._femmesh_softHist0
```

Local targeted pull:

```text
_analysis_fem_mechanism_20260528/taobo_event_checkpoint_pull/femmesh_softHist0/
```

The matched-event comparison does not rescue the mechanism gap.  Against FEM
c69, PIDL event-state damage is still near the FEM tip field, and raw `psi+`
is high, but active/degraded `psi+` becomes even smaller:

| Reference | PIDL state | damage tip2 | alpha_bar tip2 | alpha_bar p99 | raw psi tip2 | active psi tip2 | active psi p99 |
|---|---|---:|---:|---:|---:|---:|---:|
| standard FEM c69 | fixed j68 | 1.012 | 0.466 | 0.191 | 1.330 | 0.0287 | 1.374 |
| standard FEM c69 | detected j84 | 1.027 | 0.469 | 0.198 | 1.420 | 0.0058 | 0.012 |
| standard FEM c69 | confirmed j85 | 1.027 | 0.469 | 0.198 | 1.420 | 0.0066 | 0.012 |
| n_step10 FEM c69 | fixed j68 | 1.008 | 0.373 | 0.202 | 1.583 | 0.0233 | 1.299 |
| n_step10 FEM c69 | detected j84 | 1.013 | 0.368 | 0.210 | 1.648 | 0.0046 | 0.011 |
| n_step10 FEM c69 | confirmed j85 | 1.013 | 0.368 | 0.210 | 1.648 | 0.0052 | 0.011 |

The event-state result is important: simply waiting until PIDL reaches its own
fracture event does not recover the active-driver mechanism.  PIDL's raw tensile
field is not too weak; the degraded active work is almost extinguished around
the near-tip probes after damage saturation.

## Decision

The remaining gap is not mainly a FEM substep-count/reference-choice artifact,
nor a fixed-cycle-only artefact.  Using n_step10 as the FEM reference gives the
same conclusion as standard FEM, and using PIDL event states makes the active
driver gap sharper.  The next useful PIDL experiments should target the local
active-driver/history feedback, not another FEM cadence alignment.

## Next PIDL Gate

Before launching a broad sweep, require each new PIDL candidate to export or be
posthoc-scored on:

```text
c20/c40/c69 fixed-cycle common probes
matched-event field probes when checkpoint archive is available
damage tip2 and width
alpha_bar tip2, p99, p999
raw psi+ tip2
active psi+ tip2
Delta E_d from c1
right-boundary/event count
```

Best next experiments:

1. Controlled staged-alpha or local-first/domain-decomposed patch run under the
   strict FEM-mesh soft-hist0 setup.
2. Promote only if it improves `alpha_bar` and active `psi+` near the tip
   without simply over-damaging the right boundary.
3. Keep raw `psi+` and active/degraded `psi+` separated in every figure and
   table; raw agreement alone is now known to be insufficient.

## 2026-06-01 Strict Staged-Head Test

The first strict FEM-mesh staged-head discriminator is running on Taobo from
commit `730101a`:

```text
workspace: /mnt/data2/drtao/projects/pidl-staged-head-730101a
archive:   /mnt/data2/drtao/pidl_archives/staged_head_730101a
GPU:       4
PID:       1001885
log:       /mnt/data2/drtao/projects/pidl-staged-head-730101a/SENS_tensile/run_logs/femmesh_stagedHead_uv10lr_headgrad_u012_N100_seed1_20260601.log
runner:    SENS_tensile/run_fem_mesh_staged_alpha_umax.py
mesh:      meshed_geom_fem_soft_hist0.msh
schedule:  uv-head 750 RPROP lr=1e-4, alpha-head 750 RPROP lr=1e-5, joint 10000
cycles:    N100, Umax=0.12, seed=1, fracture confirm cycles=3
diagnostic cycles: 0,1,2,3,20,40,69,80,99
```

The staged-head implementation is a controlled optimizer-path intervention,
not a new physics objective.  The current network still has a shared trunk and
one final output layer; the "uv head" and "alpha head" are the corresponding
rows of that output layer.  Therefore this test can strengthen/slacken the
output-head stages with separate optimizers or learning rates, but it is not a
true independently learned trunk for displacement and damage.

From this point, future FEM/PIDL alignment diagnostics should keep exporting
the head-wise gradient table for `E_el`, `E_d`, and `E_hist` (`all`,
`trunk_shared`, `uv_head`, `alpha_head`).  This is needed to distinguish
whether a candidate is solving elastic relaxation through the displacement
rows or mostly changing the damage row to reduce the coupled energy.
