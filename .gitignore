# ── 系统文件 ─────────────────────────────────────────────
.DS_Store
*__pycache__*

# ── 训练输出目录（hl_* 命名规则）────────────────────────
hl_*/

# ── 模型权重 & 损失数据（实验结果）──────────────────────
*.pt
*.npy
*.npz

# ── 图片输出 ─────────────────────────────────────────────
*.png
*.pdf

# ── TensorBoard 日志 ─────────────────────────────────────
TBruns/
events.out.tfevents.*

# ── 网格文件（由 gmsh 生成，非手写代码）────────────────
*.msh

# ── 训练日志 ─────────────────────────────────────────────
*.log
nohup.out

# ── 压缩包 ───────────────────────────────────────────────
*.zip

# ── Claude Code 内部文件（本地配置、worktree）────────────
.claude/

# ── 结果/草稿（原则：结论进 memory，不进 git；见 CLAUDE.md）
paper_draft/
notes/
findings.md
# 生成的图（.png 已 gitignore，但子目录也要排除防止 README/txt 等误入）
SENS_tensile/figures/

# ── 生成型结果表（保留少量 canonical CSV，忽略脚本导出的摘要）────
SENS_tensile/*_summary.csv
SENS_tensile/posthoc_*.csv
SENS_tensile/trajectory_*.csv
SENS_tensile/*_per_criterion.csv
SENS_tensile/audit_active_driver_results.csv

# ── 本地迁移/备份残留（不作为共享文档）────────────────────
docs/_backups/

# ── 本地研究分析/briefing（不作为共享 workflow 资产）──────
docs/advisor_method_evolution.md
docs/diagoosis.md
docs/fem_likeness_scorecard.md
