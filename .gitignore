# ── 系统文件 ─────────────────────────────────────────────
.DS_Store
*__pycache__*

# ── 训练输出目录（hl_* 命名规则）────────────────────────
hl_*/

# ── 模型权重 & 损失数据（实验结果）──────────────────────
*.pt
*.npy

# ── 图片输出 ─────────────────────────────────────────────
*.png
*.pdf

# ── TensorBoard 日志 ─────────────────────────────────────
TBruns/
events.out.tfevents.*

# ── 网格文件（由 gmsh 生成，非手写代码）────────────────
*.msh

# ── 训练日志 ─────────────────────────────────────────────
*.log
nohup.out

# ── 压缩包 ───────────────────────────────────────────────
*.zip

# ── Claude Code 内部文件（本地配置、worktree）────────────
.claude/

# ── 结果/草稿（原则：结论进 memory，不进 git；见 CLAUDE.md）
paper_draft/
notes/
findings.md
